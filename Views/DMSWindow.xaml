<Window x:Class="Top5.Views.DMSWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Contrôle Démarrage Série (DMS)" Width="450" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" Background="#FAFAFA">

	<Grid Margin="20">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<TextBlock Grid.Row="0" Text="{Binding Title}" FontSize="16" FontWeight="Bold" Foreground="#005A9E" Margin="0,0,0,15" TextWrapping="Wrap"/>

		<TextBlock Grid.Row="1" Text="Cochez obligatoirement tous les points de contrôle :" FontStyle="Italic" Foreground="#555555" Margin="0,0,0,10"/>

		<Border Grid.Row="2" Background="White" BorderBrush="#DDDDDD" BorderThickness="1" CornerRadius="4" Padding="15">
			<StackPanel>
				<CheckBox IsChecked="{Binding Pt1}" Content="Documents et moyens d'auto-contrôle au poste" Margin="0,0,0,10" FontSize="13"/>
				<CheckBox IsChecked="{Binding Pt2}" Content="Dateur et micropercussion à jour" Margin="0,0,0,10" FontSize="13"/>
				<CheckBox IsChecked="{Binding Pt3}" Content="Aspect de fonderie" Margin="0,0,0,10" FontSize="13"/>
				<CheckBox IsChecked="{Binding Pt4}" Content="Conformité découpe / Parachèvement" Margin="0,0,0,10" FontSize="13"/>
				<CheckBox IsChecked="{Binding Pt5}" Content="Dimensionnel" Margin="0,0,0,10" FontSize="13"/>
				<CheckBox IsChecked="{Binding Pt6}" Content="Santé matière" Margin="0,0,0,10" FontSize="13"/>
				<CheckBox IsChecked="{Binding Pt7}" Content="Étanchéité" Margin="0,0,0,10" FontSize="13"/>
				<CheckBox IsChecked="{Binding Pt8}" Content="Modèle de limage au poste" Margin="0,0,0,10" FontSize="13"/>
			</StackPanel>
		</Border>

		<StackPanel Grid.Row="3" Margin="0,15,0,0">

			<Grid Margin="0,0,0,15">
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="*"/>
					<ColumnDefinition Width="*"/>
				</Grid.ColumnDefinitions>

				<StackPanel Grid.Column="0" Margin="0,0,10,0">
					<TextBlock Text="Date de validation :" FontWeight="SemiBold" Margin="0,0,0,5"/>
					<DatePicker SelectedDate="{Binding SelectedDate, UpdateSourceTrigger=PropertyChanged}" Height="28"/>
				</StackPanel>

				<StackPanel Grid.Column="1">
					<TextBlock Text="Contrôleur / Valideur :" FontWeight="SemiBold" Margin="0,0,0,5"/>
					<TextBox Text="{Binding Valideur, UpdateSourceTrigger=PropertyChanged}" Height="28" Padding="4,0" FontSize="14"/>
				</StackPanel>
			</Grid>

			<Grid>
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="*"/>
					<ColumnDefinition Width="*"/>
				</Grid.ColumnDefinitions>
				<Button Grid.Column="0" Content="Annuler" Height="35" Margin="0,0,5,0" Click="BtnAnnuler_Click" Background="White"/>
				<Button Grid.Column="1" Content="Valider le DMS" Command="{Binding ValidateCommand}" IsEnabled="{Binding CanValidate}"
                        Height="35" Margin="5,0,0,0" Background="#2ECC71" Foreground="White" FontWeight="Bold"/>
			</Grid>
		</StackPanel>
	</Grid>
</Window>